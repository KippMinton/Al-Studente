@model AlStudente.Models.TeacherNoteViewModel

@{
    ViewData["Title"] = "Edit Note";
}

<h1>Edit</h1>

<h4>TeacherNoteViewModel</h4>
<hr />
@if (Model.Teacher.Id == Model.TeacherNote.TeacherId)
{
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <input asp-for="TeacherNote.Id" type="hidden" />
                </div>
                <div class="form-group">
                    <label asp-for="TeacherNote.Title" class="control-label"></label>
                    <input asp-for="TeacherNote.Title" class="form-control" />
                    <span asp-validation-for="TeacherNote.Title" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="TeacherNote.Content" class="control-label"></label>
                    <input asp-for="TeacherNote.Content" class="form-control" />
                    <span asp-validation-for="TeacherNote.Content" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
}
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
